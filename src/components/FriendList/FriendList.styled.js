import styled from 'styled-components';

export const ItemList = styled.ul`
   display: flex;
   gap: 15px;
   align-items: center;
   flex-direction: column;
   margin-top: 60px;
   padding: 40px;
   list-style: none;
`;
